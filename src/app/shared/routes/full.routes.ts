import { Routes } from '@angular/router';

// The theme demo pages this used to route to (error-page, authentication,
// coming-soon, maintenance) were removed in the UI re-skin - see
// components/apps/admin/** for the real application routes (routes.ts).
export const full: Routes = [];
